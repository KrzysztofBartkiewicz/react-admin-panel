export const getAnchor = (state) => state.anchor;
export const getOrders = (state) => state.orders;
export const getCurrentCustomerId = (state) => state.getCurrentCustomerId;
export const isModalOpen = (state) => state.isModalOpen;
